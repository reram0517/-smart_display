# スマートディスプレイ Web版

AndroidやCapacitorを使わず、Webブラウザだけで動作するスマートディスプレイです。

## 起動

```powershell
npm start
```

ブラウザで `http://localhost:3000` を開きます。

天気情報は気象庁、日本気象協会、および時間別気温データを利用します。現在地を使う場合は、ブラウザの位置情報許可を有効にしてください。
